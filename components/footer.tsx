import type { NextPage } from "next";

const Footer: NextPage = () => {
    return (
        <div className="header mt-10">
            <ul className="text-blue-400">
                <li className="cursor-pointer underline">Books</li>
            </ul>
        </div>
    );
};

export default Footer;
